using Microsoft.AspNetCore.Identity;

namespace HistoryBackend.Models;

// Наследуем от IdentityUser
public class ApplicationUser : IdentityUser
{
    // Можно добавить любые дополнительные поля (пока их нет)
}